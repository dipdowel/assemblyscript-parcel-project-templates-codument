{
  "name": "template-canvas-2d-resizable",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "clean:dist": "rimraf dist/*",
    "clean:build": "rimraf build/*",
    "start": "yarn clean:dist && parcel src/index.html",
    "build": "yarn clean:dist && parcel build src/index.html"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.9.1",
    "parcel": "^2.9.1",
    "parcel-config-assemblyscript-codument": "^0.1.6",
    "parcel-transformer-assemblyscript-codument": "^0.1.3",
    "prettier": "^2.8.8",
    "rimraf": "^5.0.0"
  }
}
