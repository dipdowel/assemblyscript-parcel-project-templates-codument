{
  "name": "template-canvas-2d-resizable",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "clean:dist": "rimraf dist/*",
    "clean:build": "rimraf build/*",
    "start": "yarn clean:dist && parcel src/index.html",
    "build": "yarn clean:dist && parcel build src/index.html",
    "build-subdir": "yarn clean:dist && parcel build --public-url . src/index.html"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.10.3",
    "parcel": "2.10.3",
    "parcel-config-assemblyscript-codument": "0.4.0",
    "parcel-transformer-assemblyscript-codument": "0.4.0",
    "prettier": "^3.0.3",
    "rimraf": "^5.0.1",
    "cosmiconfig": "8.3.6"
  },
  "resolutions": {
    "cosmiconfig": "8.3.6"
  },
  "overrides": {
    "cosmiconfig": "8.3.6"
  }
}
